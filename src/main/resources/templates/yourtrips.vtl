<!DOCTYPE html>
<html>
  <head>
    <title>GetPacked!</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <h1>Your trips!</h1>

<table class="table table-hover">

        <tbody>
          #foreach( $trips in $trips )
          <tr>
            <th scope="row">
            Trip name: $trips.trip_name,
            Destination: $trips.destination,
            Hotel name: $trips.hotel_name
            <form method="get" action="/schedule">
            <button type="submit" name="trip_name" value=$trips.trip_name class="btn btn-primary" >View</button>
           #end
           </form>
        </tbody>
      </table>

  </body>
</html>